import {element} from '../Models/elements';

export default function loader(){
    element.loader.style.display = 'none';
    element.search.style.display = "flex";
    element.weatherContainer.style.display = 'block';

}